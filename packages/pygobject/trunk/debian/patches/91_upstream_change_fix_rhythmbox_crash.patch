# Description: upstream bugzilla change to fix a crasher
# Upstream: http://bugzilla.gnome.org/show_bug.cgi?id=575781
#
Index: pygobject/gio/gio.override
===================================================================
--- pygobject/gio/gio.override	(revision 1026)
+++ pygobject/gio/gio.override	(working copy)
@@ -145,8 +145,12 @@
 {
     if (notify) {
         if (notify->referenced) {
+            PyGILState_STATE state;
+
+            state = pyg_gil_state_ensure();
             Py_XDECREF(notify->callback);
             Py_XDECREF(notify->data);
+            pyg_gil_state_release(state);
         }
 
         if (notify->buffer)

